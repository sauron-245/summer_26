for (hobo in hobos_week) {
update_hobo_log(hobo)
}
spec()
update_hobo_log("P1_creek")
log_new <- read_excel(paste0("hobos_upload/", "P1_creek", ".xlsx")) %>%  # Most recently downloaded data
rename_with(~ trimws(sub("\\(.*$", "", .x))) %>% # Correctly format column names and get rid of metadata columns
select(any_of(cols_to_check))
cols_to_check = c("Date-Time", "Temperature", "Electrical Conductivity", "Specific Conductivity", "Salinity", "Total Dissolved Solids")
log_new <- read_excel(paste0("hobos_upload/", "P1_creek", ".xlsx")) %>%  # Most recently downloaded data
rename_with(~ trimws(sub("\\(.*$", "", .x))) %>% # Correctly format column names and get rid of metadata columns
select(any_of(cols_to_check))
log_new <- read_excel(paste0("hobos_upload/", "P1_creek", ".xlsx")) %>%  # Most recently downloaded data
rename_with(~ trimws(sub("\\(.*$", "", .x)))  # Correctly format column names and get rid of metadata columns
names(log_new)
update_hobo_log = function(site) {
cols_to_check = c("Date-Time", "Temperature", "Temperature , °C", "Electrical Conductivity", "Specific Conductivity", "Salinity", "Total Dissolved Solids")
log_standing <- read_csv(paste0("hobos_running/", site, "_current.csv")) # Running total of log data
log_new <- read_excel(paste0("hobos_upload/", site, ".xlsx")) %>%  # Most recently downloaded data
rename_with(~ trimws(sub("\\(.*$", "", .x))) %>% # Correctly format column names and get rid of metadata columns
select(any_of(cols_to_check)) # References list of columns of interest and selects just columns containing real data, removing metadata/empty columns
if (length(names(log_new)) == 0) {
stop(paste0("Check column names/format at site ", site, ".")) # This will hopefully catch issues caused by mismatched column names between the running .csv and newly uploaded data
}
if ("Temperature , °C" %in% names(log_new)){
log_new = log_new %>% rename(Temperature = `Temperature , °C`)
}
date_current_last = as_datetime(log_standing[[nrow(log_standing),1]])
date_new = as_datetime(log_new[[nrow(log_new),1]])
if (date_current_last == date_new) { # Checks if running log data are already up to date
stop(paste0("Most recent date of running log data for site ", site, " matches last date on added data. Have you already merged these dataframes?"))
}
data_to_add = log_new %>%
filter(`Date-Time` > date_current_last) # selects only data not present in running logs
log_standing_updated = rbind(log_standing, data_to_add)
write_csv(log_standing_updated, paste0("hobos_running/", site, "_current.csv")) # Adds new data and overwrites existing .csv
}
update_hobo_log = function(site) {
cols_to_check = c("Date-Time", "Temperature", "Temperature , °C", "Electrical Conductivity", "Specific Conductivity", "Salinity", "Total Dissolved Solids")
log_standing <- read_csv(paste0("hobos_running/", site, "_current.csv")) # Running total of log data
log_new <- read_excel(paste0("hobos_upload/", site, ".xlsx")) %>%  # Most recently downloaded data
rename_with(~ trimws(sub("\\(.*$", "", .x))) %>% # Correctly format column names and get rid of metadata columns
select(any_of(cols_to_check)) # References list of columns of interest and selects just columns containing real data, removing metadata/empty columns
if (length(names(log_new)) == 0) {
stop(paste0("Check column names/format at site ", site, ".")) # This will hopefully catch issues caused by mismatched column names between the running .csv and newly uploaded data
}
if ("Temperature , °C" %in% names(log_new)){
log_new = log_new %>% rename(Temperature = `Temperature , °C`)
}
date_current_last = as_datetime(log_standing[[nrow(log_standing),1]])
date_new = as_datetime(log_new[[nrow(log_new),1]])
if (date_current_last == date_new) { # Checks if running log data are already up to date
stop(paste0("Most recent date of running log data for site ", site, " matches last date on added data. Have you already merged these dataframes?"))
}
data_to_add = log_new %>%
filter(`Date-Time` > date_current_last) # selects only data not present in running logs
log_standing_updated = rbind(log_standing, data_to_add)
write_csv(log_standing_updated, paste0("hobos_running/", site, "_current.csv")) # Adds new data and overwrites existing .csv
}
update_hobo_log("P1_creek")
update_hobo_log("P2")
check_csv("P1_creek")
check_csv = function(site) { # Use this to make graphs of temperature over time. Intended to check that dates/expected data gaps are behaving as they ought.
file = read_csv(paste0("hobos_running/", site, "_current.csv"))
file %>%
ggplot(aes(x = `Date-Time`, y = Temperature)) +
geom_line() +
labs(title = paste0("Temperature at ", site, " from ", min(file$`Date-Time`), " to ", max(file$`Date-Time`)))
}
check_csv("P1_creek")
update_hobo_log("P2")
check_csv("P2")
update_hobo_log("P2_creek")
check_csv("P2_creek")
update_hobo_log("P3")
check_csv("P3")
update_hobo_log("P4")
check_csv("P4")
update_hobo_log("P4_creek")
check_csv("P4_creek")
update_hobo_log("P5")
check_csv("P5")
update_hobo_log("P5_creek")
check_csv("P5_creek")
update_hobo_log("P5_creek_cond")
check_csv("P5_creek_cond")
check_csv("Dwnstrm")
library(tidyverse)
library(tidyverse)
library(lubridate)
library(readxl)
library(ggplot2)
check_csv = function(site) { # Use this to make graphs of temperature over time. Intended to check that dates/expected data gaps are behaving as they ought.
file = read_csv(paste0("hobos_running/", site, "_current.csv"))
file %>%
ggplot(aes(x = `Date-Time`, y = Temperature)) +
geom_line() +
labs(title = paste0("Temperature at ", site, " from ", min(file$`Date-Time`), " to ", max(file$`Date-Time`)))
}
check_csv("Dwnstrm")
library(tidyverse)
library(lubridate)
library(readxl)
manualdata = read_csv("Well_Sampling_Data_ALL.xlsx")
manualdata = read_excel("Well_Sampling_Data_ALL.xlsx")
View(manualdata)
library(tidyverse)
library(lubridate)
p1_baro = read_csv("p1_baro.csv")
p5_baro = read_csv("p5_baro.csv")
View(p1_baro)
View(p5_baro)
p5_baro = read_csv("p5_baro.csv")
p1_baro = read_csv("P1_baro.csv")
p1_baro = p1_baro %>%
mutate(datetime = paste(Date, Time, sep = " "))
p1_baro = p1_baro %>%
mutate(datetime = paste(Date, Time, sep = " "), datetime = as_datetime(datetime))
p1_baro = read_csv("P1_baro.csv")
p1_baro = p1_baro %>%
mutate(datetime = paste(Date, Time, sep = " "), datetime = mdy_hms(datetime))
p5_baro = p5_baro %>%
mutate(datetime = paste(Date, Time, sep = " "), datetime = mdy_hms(datetime))
ggplot(p1_baro, aes(x = datetime, y = LEVEL)) +
geom_line()
ggplot(p5_baro, aes(x = datetime, y = LEVEL)) +
geom_line()
install.packages("xml2")
install.packages("XML")
library(xml2)
library(XML)
testdata = read_xml("level_test.xl")
testdata = read_xml("level_test.xle")
test = xmlParse(testdat)
test = xmlParse(testdata)
xml_structure(testdata)
dates = xml_find_all(testdata, ".//Date")
View(dates)
dates = xml_text(xml_find_all(testdata, ".//Date"))
times = xml_text(xml_find_all(testdata, ".//Time"))
test
pressures = xml_text(xml_find_all(testdata, ".//Ch1"))
pressures = xml_text(xml_find_all(testdata, ".//ch1"))
temps = xml_text(xml_find_all(testdata, ".//ch2"))
test_df = tibble(Date = dates, Time = times, Pressure = pressures, Temp = temps)
dates = dates[2:4046]
test_df = tibble(Date = dates, Time = times, Pressure = pressures, Temp = temps)
times = times[2:4046]
test_df = tibble(Date = dates, Time = times, Pressure = pressures, Temp = temps)
test_df = tibble(Date = dates, Time = times, Pressure = pressures, Temp = temps) %>%
as_data_frame()
View(test_df)
test_df = tibble(Date = dates, Time = times, Pressure = pressures, Temp = temps) %>%
data.frame()
View(test_df)
# install.packages("xml2")
# install.packages("XML")
library(tidyverse)
library(lubridate)
library(xml2)
library(XML)
testdata = read_xml("level_test.xle")
test = xmlParse(testdata)
dates = xml_text(xml_find_all(testdata, ".//Date"))
times = xml_text(xml_find_all(testdata, ".//Time"))
pressures = xml_text(xml_find_all(testdata, ".//ch1"))
items_to_pull = c("Date", "Time", "ch1", "ch2")
cols_of_interest = c("Date", "Time", "Pressure", "Temperature")
xle_file = xmlParse(testdata)
for (i in range(length(items_to_pull))) {
cols_of_interest[as.integer(i)] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
print(cols_of_interest[as.integer(i)])
}
i = 1
cols_of_interest[i]
print(paste0(".//", items_to_pull[i]))
cols_of_interest[i] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
dates = xml_text(xml_find_all(testdata, ".//Date"))
class(xle_file)
xle_file = read_xml("level_test.xle")
cols_of_interest[i] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
dates = xml_text(xml_find_all(xle_file, ".//Date"))
class(xle_file)
xml_structure(xle_file)
cols_of_interest[i] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
length(xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i]))))
length(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
cols_of_interes[i] = tibble(xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i]))))
cols_of_interest[i] = tibble(xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i]))))
xle_file = read_xml("level_test.xle")
items_to_pull = c("Date", "Time", "ch1", "ch2")
cols_of_interest = c("Date", "Time", "Pressure", "Temperature")
i = 1
cols_of_interest[i] = tibble(xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i]))))
for (i in range(length(items_to_pull))) {
cols_of_interest[i] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
print(cols_of_interest[as.integer(i)])
}
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
xle_data = data.frame(cols_of_interest)
xle_file = read_xml("P5_8_31.xle")
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
xle_data = data.frame(cols_of_interest)
dates = xml_text(xml_find_all(xle_file, ".//Date"))
times = xml_text(xml_find_all(xle_file, ".//Time"))
pressures = xml_text(xml_find_all(xle_file, ".//ch1"))
temps = xml_text(xml_find_all(xle_file, ".//ch2"))
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
cols = c("Date", "Time", "Pressure", "Temperature")
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in 1:3) {
if nrow(cols[[i]] != nrow(cols[[4]]))
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
cols = c("Date", "Time", "Pressure", "Temperature")
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:3)) {
if (nrow(cols[[i]] != nrow(cols[[4]]))){
cols[[i]] = cols[[i]][1:(nrow(cols[[i]]) - 1)]
}
}
}
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
cols = c("Date", "Time", "Pressure", "Temperature")
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:3)) {
if (nrow(cols[[i]] != nrow(cols[[4]]))){
cols[[i]] = cols[[i]][1:(nrow(cols[[i]]) - 1)]
}
}
}
handle_xle("P5_8_31")
xle_file = read_xml("P5_8_31.xle")
for (i in seq_along(items_to_pull)) {
a sd f
for (i in seq_along(items_to_pull)) {
cols[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
items_to_pull = c("Date", "Time", "ch1", "ch2")
cols = c("Date", "Time", "Pressure", "Temperature")
for (i in seq_along(items_to_pull)) {
cols[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
cols_of_interest = c("Date", "Time", "Pressure", "Temperature")
xle_file = read_xml("P5_8_31.xle")
items_to_pull = c("Date", "Time", "ch1", "ch2")
result = c("Date", "Time", "Pressure", "Temperature")
cols_of_interest = vector("list", length(itmes_to_pull))
names(cols_of_interst) = result
cols_of_interest = vector("list", length(items_to_pull))
names(cols_of_interest) = result
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
result = c("Date", "Time", "Pressure", "Temperature")
cols_of_interest = vector("list", length(items_to_pull))
names(cols_of_interest) = result
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:3)) {
if (nrow(cols_of_interest[[i]] != nrow(cols_of_interest[[4]]))){
cols_of_interest[[i]] = cols_of_interest[[i]][2:(nrow(cols_of_interest[[i]]) - 1),]
}
}
}
handle_xle("P5_8_31")
for (i in (1:3)) {
if (length(cols_of_interest[[i]]) != length(cols_of_interest[[4]])){
cols_of_interest[[i]] = cols_of_interest[[i]][2:(length(cols_of_interest[[i]]) - 1)]
}
}
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:3)) {
if (length(cols_of_interest[[i]]) != length(cols_of_interest[[4]])){
cols_of_interest[[i]] = cols_of_interest[[i]][2:(length(cols_of_interest[[i]]))]
}
}
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
result = c("Date", "Time", "Pressure", "Temperature")
cols_of_interest = vector("list", length(items_to_pull))
names(cols_of_interest) = result
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:3)) {
if (length(cols_of_interest[[i]]) != length(cols_of_interest[[4]])){
cols_of_interest[[i]] = cols_of_interest[[i]][2:(length(cols_of_interest[[i]]))]
}
}
}
handle_xle("P5_8_31")
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
result = c("Date", "Time", "Pressure", "Temperature")
cols_of_interest = vector("list", length(items_to_pull))
names(cols_of_interest) = result
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:3)) {
if (length(cols_of_interest[[i]]) != length(cols_of_interest[[4]])){
cols_of_interest[[i]] = cols_of_interest[[i]][2:(length(cols_of_interest[[i]]))]
}
}
filename = paste0(location, " data")
filename = as.data.frame(cols_of_interest)
}
handle_xle("P5_8_31")
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
result = c("Date", "Time", "Pressure", "Temperature")
cols_of_interest = vector("list", length(items_to_pull))
names(cols_of_interest) = result
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:3)) {
if (length(cols_of_interest[[i]]) != length(cols_of_interest[[4]])){
cols_of_interest[[i]] = cols_of_interest[[i]][2:(length(cols_of_interest[[i]]))]
}
}
df = as.data.frame(cols_of_interest, stringsAsFactors = FALSE)
return(df)
}
handle_xle("P5_8_31")
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
result = c("Date", "Time", "Pressure", "Temperature")
cols_of_interest = vector("list", length(items_to_pull))
names(cols_of_interest) = result
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:3)) {
if (length(cols_of_interest[[i]]) != length(cols_of_interest[[4]])){
cols_of_interest[[i]] = cols_of_interest[[i]][2:(length(cols_of_interest[[i]]))]
}
}
df = as.data.frame(cols_of_interest, stringsAsFactors = FALSE)
}
handle_xle("P5_8_31")
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
# result = c("Date", "Time", "Pressure", "Temperature")
# cols_of_interest = vector("list", length(items_to_pull))
# names(cols_of_interest) = result
cols_of_interest = c("Date", "Time", "Pressure", "Temperature")
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:2)) { # Some files apparently have an extra date/time with no data attached. Not sure why this happens. This should catch that issue.
if (length(cols_of_interest[[i]]) != length(cols_of_interest[[4]])) {
cols_of_interest[[i]] = cols_of_interest[[i]][2:(length(cols_of_interest[[i]]))]
}
}
df = as.data.frame(cols_of_interest, stringsAsFactors = FALSE)
}
handle_xle("P5_8_31")
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
result = c("Date", "Time", "Pressure", "Temperature")
cols_of_interest = vector("list", length(items_to_pull))
names(cols_of_interest) = result
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:2)) { # Some files apparently have an extra date/time with no data attached. Not sure why this happens. This should catch that issue.
if (length(cols_of_interest[[i]]) != length(cols_of_interest[[4]])) {
cols_of_interest[[i]] = cols_of_interest[[i]][2:(length(cols_of_interest[[i]]))]
}
}
df = as.data.frame(cols_of_interest, stringsAsFactors = FALSE)
}
handle_xle("P5_8_31")
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
result = c("Date", "Time", "Pressure", "Temperature")
cols_of_interest = vector("list", length(items_to_pull))
names(cols_of_interest) = result
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:2)) { # Some files apparently have an extra date/time with no data attached. Not sure why this happens. This should catch that issue.
if (length(cols_of_interest[[i]]) != length(cols_of_interest[[4]])) {
cols_of_interest[[i]] = cols_of_interest[[i]][2:(length(cols_of_interest[[i]]))]
}
}
df = as.data.frame(cols_of_interest, stringsAsFactors = FALSE)
return(df)
}
handle_xle("P5_8_31")
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
result = c("Date", "Time", "Pressure", "Temperature")
cols_of_interest = vector("list", length(items_to_pull))
names(cols_of_interest) = result
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:2)) { # Some files apparently have an extra date/time with no data attached. Not sure why this happens. This should catch that issue.
if (length(cols_of_interest[[i]]) != length(cols_of_interest[[4]])) {
cols_of_interest[[i]] = cols_of_interest[[i]][2:(length(cols_of_interest[[i]]))]
}
}
df = as.data.frame(cols_of_interest, stringsAsFactors = FALSE) %>%
mutate(Date = ymd(Date), time = hms(Time))
return(df)
}
handle_xle("P5_8_31")
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
result = c("Date", "Time", "Pressure", "Temperature")
cols_of_interest = vector("list", length(items_to_pull))
names(cols_of_interest) = result
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:2)) { # Some files apparently have an extra date/time with no data attached. Not sure why this happens. This should catch that issue.
if (length(cols_of_interest[[i]]) != length(cols_of_interest[[4]])) {
cols_of_interest[[i]] = cols_of_interest[[i]][2:(length(cols_of_interest[[i]]))]
}
}
df = as.data.frame(cols_of_interest, stringsAsFactors = FALSE) %>%
mutate(Date = ymd(Date),Time = as.Time(Time))
return(df)
}
handle_xle("P5_8_31")
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
result = c("Date", "Time", "Pressure", "Temperature")
cols_of_interest = vector("list", length(items_to_pull))
names(cols_of_interest) = result
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:2)) { # Some files apparently have an extra date/time with no data attached. Not sure why this happens. This should catch that issue.
if (length(cols_of_interest[[i]]) != length(cols_of_interest[[4]])) {
cols_of_interest[[i]] = cols_of_interest[[i]][2:(length(cols_of_interest[[i]]))]
}
}
df = as.data.frame(cols_of_interest, stringsAsFactors = FALSE) %>%
mutate(Date = ymd(Date),Time = as.time(Time))
return(df)
}
handle_xle("P5_8_31")
handle_xle = function(location) {
items_to_pull = c("Date", "Time", "ch1", "ch2")
result = c("Date", "Time", "Pressure", "Temperature")
cols_of_interest = vector("list", length(items_to_pull))
names(cols_of_interest) = result
path = paste0(location, ".xle")
xle_file = read_xml(path)
for (i in seq_along(items_to_pull)) {
cols_of_interest[[i]] = xml_text(xml_find_all(xle_file, paste0(".//", items_to_pull[i])))
}
for (i in (1:2)) { # Some files apparently have an extra date/time with no data attached. Not sure why this happens. This should catch that issue.
if (length(cols_of_interest[[i]]) != length(cols_of_interest[[4]])) {
cols_of_interest[[i]] = cols_of_interest[[i]][2:(length(cols_of_interest[[i]]))]
}
}
df = as.data.frame(cols_of_interest, stringsAsFactors = FALSE) %>%
mutate(Date = ymd(Date))
return(df)
}
handle_xle("P5_8_31")
